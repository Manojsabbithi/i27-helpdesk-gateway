apiVersion: v1
kind: Service
metadata:
  name: i27-helpdesk-gateway
  namespace: i27-helpdesk-dev

spec:
  selector:
    app: i27-helpdesk-gateway

  ports:
    - name: http
      port: 80
      targetPort: 8080

  type: ClusterIP
